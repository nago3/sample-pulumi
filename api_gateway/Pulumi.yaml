name: api-gateway-lambda
runtime:
  name: python
  options:
    virtualenv: venv
description: API Gateway
template:
    config:
      aws:region:
        description: AWS region
        default: ap-northeast-1
